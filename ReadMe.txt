 
 ==============================================================================
 file system 
 ==============================================================================
--ROOT
    |
    |- core: logger.py, config.py, file_operations.py
    |- eye_analyzer: analyzer.py (answer to question1)
    |- eye_detector: detector.py (answer to question2)
    |- logs:
            |- analyzing logs.
            |- detecting logs.
    |- saved_results (contains the results from the detection phase).
    |- sources model.xml file
    |- requirments.txt
    
 ==============================================================================
 Requirements
 ==============================================================================
for pip installation use: requirments.txt
for conda env use: requirments_conda.yml